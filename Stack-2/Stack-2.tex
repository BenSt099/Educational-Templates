\documentclass[12pt,a4paper]{article}

\usepackage{genstacks}

\begin{document}

\begin{stack}
    {5}{1,2,3,4,5}{blue-300}{blue-600}{false} 
    % 1. arg: Number of elements
    % 2. arg: All elements, sorted from bottom to top (5 will be placed at top of stack)
    % 3. arg: Color of all elements on the stack
    % 4. arg: Color of the element that is top of stack 
    %         (if you don't want a different color for the top element, set this arg to 'false')
    % 5. arg: Arrow that points to top of stack element
    %         (if you don't want the arrow, set this arg to 'false')
\end{stack}
\vspace*{10mm}

\begin{stack}
    {5}{1,2,3,4,5}{blue-300}{false}{true}
\end{stack}

\end{document}